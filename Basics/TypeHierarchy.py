'''Subset
Numbers:
>Integral
    -> integers, booleans
>Non-integarl
    -> floats(c doubles), complex, decimals, fractions

Collections:
>Sequences:
    mutable -> lists
    immutable -> tuples, strings
>Sets:
    mutable-> sets
    immutable->forzen sets
>Mappings:
    dictionaries

Callables:
>user defined functions
>generators
>classes
>instance methods
>class instances(__call__())
>built-in functions
>built-in methods

singletons:
>none
>notimplemented
>ellipsis(...)


'''

